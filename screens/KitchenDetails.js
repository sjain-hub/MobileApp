import React from "react";
import {
    StyleSheet,
    SafeAreaView,
    View,
    Text,
    Image,
    Animated,
    Dimensions,
    FlatList,
    Button,
    Pressable,
    Switch,
    SectionList,
    TouchableOpacity,
    TextInput
} from "react-native";

const KitchenDetails = ({ route, navigation }) => {
    return (
        <SafeAreaView style={styles.container}>
            <Text>ajwufgjkfgiauwegk</Text>
        </SafeAreaView>
    )
}

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: "white"
    },
    shadow: {
        shadowColor: "#000",
        shadowOffset: {
            width: 0,
            height: 3,
        },
        shadowOpacity: 0.25,
        shadowRadius: 4,
        elevation: 5,
    }
})

export default KitchenDetails;